package com.example.kotlinapp1.oops

//its only defines one object
//its cab have properties and methods same as other classes
//this object type can have only a single instance
//object declaration
object Cache{
    val name:String="HyperCache"
    fun retrieveDate():Int{
        return 0
    }
}